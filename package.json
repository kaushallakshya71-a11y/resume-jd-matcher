{
  "name": "resumematch-ai",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "description": "Resume & Job Description Matcher — AI-powered compatibility analyzer",
  "scripts": {
    "start": "serve . --listen $PORT",
    "dev": "serve .",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android",
    "cap:run": "npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^8.2.0",
    "@capacitor/cli": "^7.6.0",
    "@capacitor/core": "^8.2.0",
    "serve": "^14.2.1"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}
